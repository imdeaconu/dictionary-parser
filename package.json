{
  "name": "dictionary-parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "rimraf ./build && tsc && tsc-alias",
    "start-wiktionary": "npm run build && node build/examples/wiktionary.example.js",
    "start-in-memory": "npm run build && node build/examples/in-memory.example.js",
    "start-rest": "npm run build && node build/examples/rest.example.js"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/http-server": "^0.12.4",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "tsc-alias": "^1.8.8",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.4.1"
  }
}
